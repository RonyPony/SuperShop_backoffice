export interface Product {
  name: string;
  code: string;
  description: string;
  price: number;
  stock: number;
  imageUrl: string;
  branchId: string;
  id: string;
  createdAt: Date;
  updatedAt: Date;
}
